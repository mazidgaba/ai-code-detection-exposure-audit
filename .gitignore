# Corpora, model weights and anything else downloaded or trained. None of it is
# ours to redistribute, and it is several gigabytes.
aicd/artifacts/
*.parquet
*.pt
*.bin
*.safetensors
*.pkl
*.ckpt

# Credentials. Never commit any of these.
.kaggle/
kaggle.json
.env
.env.*
*.pem
*.key
id_rsa*
id_ed25519*
.netrc
credentials.json
secrets.*

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ipynb_checkpoints/

# Editors and operating systems
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# LaTeX build products
*.aux
*.bbl
*.blg
*.out
*.synctex.gz
*.fls
*.fdb_latexmk

# NOT ignored, deliberately: aicd/eval/reports/*.json and
# kaggle_runs/results/**/*.{json,npy,log}. Those are the measured results and
# the evidence behind them, so they are what makes this artifact checkable.
